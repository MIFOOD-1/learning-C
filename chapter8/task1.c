#include <stdio.h>  //task1.exe < "kakoi to fail"

int main(void)
{
    int num = 0;

    while (getchar() != EOF)
    {
        num++;
    }

printf("Kolichetvo simvilov %d", num);
    
return 0;                 
}